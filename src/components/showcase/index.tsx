function Showcase({ children }: { children: JSX.Element }) {
  return (
    <div className="mt-[200px] max-w-[740px] mx-auto text-slate-200 font-poppins">
      {children}
    </div>
  );
}

export default Showcase;
